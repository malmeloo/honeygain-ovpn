version: "3.9"

services:
  manager:
    build: .
    image: honeygain_manager
    container_name: honeygain_manager
    volumes:
      - ../configs:/app/configs
      - ./manager.py:/app/manager.py
      - hg-data:/app/data
    env_file:
      - .env

volumes:
  hg-data: